spring:
  config:
    activate:
      on-profile: test
  datasource:
    url: jdbc:postgresql://localhost:5432/test
    username: sa
    password:
    driver-class-name: org.postgresql.Driver
#jdbcTemplate sql log
logging:
  level:
    org:
      springframework:
        jdbc: debug